module.exports = {
    theme: {
      extend: {
        colors: {
          primary: {
            light: '#ffffff', // Color para modo claro
            DEFAULT: '#1f2937', // Color por defecto (puedes usarlo dinámicamente)
            dark: '#1a202c', // Color para modo oscuro
          },
        },
      },
    },
    plugins: [],
  };
  